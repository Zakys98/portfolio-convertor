name: Linting

on:
  push:

jobs:
  ruff:
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v4

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Run Ruff
        uses: astral-sh/ruff-action@v3
        with:
          version: "latest"
